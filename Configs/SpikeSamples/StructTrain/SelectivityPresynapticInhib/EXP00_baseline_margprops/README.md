## EXP00_baseline_margprops — regression with PeakMeasureMargin / DelayAgreeMarginMin props

Копия `EXP00_baseline_autothr` с явными `PeakMeasureMargin=0.06`, `DelayAgreeMarginMin=0.03`, `SyncTolerance=0.02` (legacy defaults). Оригинал не затирается.

Neuron: `NSPNeuronGen`
