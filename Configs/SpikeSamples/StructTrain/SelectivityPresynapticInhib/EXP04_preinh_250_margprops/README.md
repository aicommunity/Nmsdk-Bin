## EXP04_preinh_250_margprops — regression with PeakMeasureMargin / DelayAgreeMarginMin props

Копия `EXP04_preinh_250` с явными `PeakMeasureMargin=0.06`, `DelayAgreeMarginMin=0.03`, `SyncTolerance=0.02` (legacy defaults). Оригинал не затирается.

Neuron: `NSPNeuronGenPreinh2_5`
