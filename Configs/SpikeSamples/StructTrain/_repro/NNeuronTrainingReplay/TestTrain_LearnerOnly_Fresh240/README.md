## RU

### Baseline `NNeuronLearner` без AutoPreset

Свежий клон `TestTrain` с одним `NNeuronLearner`, `UseAutoPreset=0`, `ResetAfterLoadFlag=1`, исходными параметрами нейрона и без `NNeuronTrainer`. Для запуска используйте Release-консоль и `-t 240 -x -S`.

Этот конкретный прогон был остановлен через 2 часа 20 минут без финального сохранения: до остановки консоль ещё считала, а файлы проекта оставались исходными. Не использовать его как обученный результат; для чистого повтора скопируйте `Model_00.xml` и `Parameters_00.xml` из исходного `TestTrain`-клона.
