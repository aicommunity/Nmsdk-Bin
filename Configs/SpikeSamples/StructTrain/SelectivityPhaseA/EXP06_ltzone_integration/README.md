# EXP06 — NPulseLTZone integration

Inference-only: класс LTZone → `NPulseLTZone`, `UseLTZIntegtation=1`.

Сетка: `TimeConstant ∈ {0.001, 0.00144, 0.003}` × `Threshold ∈ {0.0115, 0.012, 0.013}`.

Сетка: `TimeConstant ∈ {0.001, 0.00144, 0.003}` × `Threshold ∈ {0.0115, 0.012, 0.013}`.

Подпапки: `Test_tc<TC>_thr<THR>/` — имя класса `NPulseLTZone` **не зарегистрировано** (fallback на Threshold).

Рабочий класс интегратора: **`NPLTZone`**. См. `Test_npltzone_tc000144_thr00115` (4/8) и `..._thr00135` (**6/8**, как EXP01).

